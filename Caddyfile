eihei.zone {
    encode gzip

    handle /api/* {
        reverse_proxy backend:8000
    }

    handle {
        reverse_proxy frontend:3000
    }
}

www.eihei.zone {
    redir https://eihei.zone{uri} permanent
}
